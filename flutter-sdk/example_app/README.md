# 小火箭 SDK 示例应用

用于测试 Rocket Workshop Auth SDK 的完整功能。

## 🚀 快速开始

### 1. 安装依赖

```bash
cd flutter-sdk/example_app
flutter pub get
```

### 2. 运行测试环境（推荐）

```bash
flutter run --dart-define=ENV=test
```

### 3. 运行线上环境

```bash
flutter run --dart-define=ENV=prod
```

---

## 📋 测试清单

### ✅ 短信登录测试

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 输入手机号，点击"发送验证码" | 收到短信验证码 |
| 2 | 输入6位验证码，点击"登录" | 登录成功，跳转到首页 |
| 3 | 查看用户信息页 | 显示用户 ID、手机号、创建时间 |

**测试环境特别注意：**
- 测试环境使用 test_public schema，数据与线上隔离
- 可以使用虚拟手机号（如 13800138000）测试

### ✅ 环境隔离测试

| 环境 | 命令 | 数据库 Schema | OSS 路径 |
|------|------|--------------|----------|
| 测试 | `--dart-define=ENV=test` | `test_public` | `test/` |
| 线上 | `--dart-define=ENV=prod` | `public` | `prod/` |

验证方法：
1. 在测试环境登录，创建用户
2. 切换到线上环境，应该是未登录状态
3. 两个环境的数据完全隔离

### ✅ 数据同步测试（待实现）

- [ ] 设备路径上传
- [ ] 用户路径上传
- [ ] 多设备数据合并
- [ ] 迁移进度显示

---

## 🔧 环境配置

### Supabase 配置（已内置）

```dart
const supabaseUrl = 'http://rocketapi.lensflow.cn';
const supabaseAnonKey = 'eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...';
```

### 切换环境

代码中使用 `Environment` 类判断当前环境：

```dart
if (Environment.isTest) {
  // 测试环境逻辑
} else {
  // 线上环境逻辑
}
```

---

## 📁 目录结构

```
lib/
├── main.dart              # 入口，环境配置
├── pages/
│   ├── login_page.dart    # 登录页
│   ├── home_page.dart     # 用户信息页
│   └── sync_demo_page.dart # 同步测试页
├── services/              # 服务层（待添加）
└── models/                # 数据模型（待添加）
```

---

## ⚠️ 已知问题

1. **CloudSyncService 未实现** - 目前只有模拟界面
2. **购买验证未接入** - Edge Function 待开发
3. **iOS/Android 真机测试** - 需要配置推送证书
